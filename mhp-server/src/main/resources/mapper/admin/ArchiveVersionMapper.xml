<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mhp.mapper.admin.ArchiveVersionMapper">
    <select id="pageQuery" resultType="com.mhp.vo.admin.ArchiveVersionVO">
        SELECT mav.*,
               su.real_name AS operatorName
        FROM mh_archive_version mav
        LEFT JOIN sys_user su ON mav.operator_id = su.user_id
        <where>
            <if test="archiveId != null">AND mav.archive_id = #{archiveId}</if>
        </where>
        ORDER BY mav.version_time DESC
    </select>
</mapper>
