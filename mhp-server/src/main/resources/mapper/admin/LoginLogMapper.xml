<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.mhp.mapper.admin.LoginLogMapper">
    <select id="pageQuery" resultType="com.mhp.vo.admin.LoginLogVO">
        SELECT sll.*,
               su.real_name AS userName
        FROM sys_login_log sll
        LEFT JOIN sys_user su ON sll.user_id = su.user_id
        <where>
            <if test="loginStatus != null">
                AND sll.login_status = #{loginStatus}
            </if>
        </where>
        ORDER BY sll.login_time DESC
    </select>

</mapper>
